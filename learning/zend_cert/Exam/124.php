
<?php

/*
When tracking upload progress with sessions, the values of 2 INI settings are needed to
determine the key in $_SESSION of the upload progress data. What are the INI settings?
(Choose 2)

A.
session.upload_progress.file

B.
session.upload_progress.key

C.
session.upload_progress.prefix

D.
session.upload_progress.freq

E.
session.upload_progress.name

Answer: C,E.
session.upload_progress.prefix, session.upload_progress.name

*/
echo '124. When tracking upload progress with sessions, the values of 2 INI settings are needed to
determine the key in $_SESSION of the upload progress data. What are the INI settings?
(Choose 2)' . PHP_EOL;
echo 'session.upload_progress.prefix, session.upload_progress.name' . PHP_EOL;